{
    'refresh_seconds' : '1',
    'schema' : '{
        fields : {
            __field : {
                type      : "bigint",
                digits    : __digits,
                indexed   : __indexed,
                sorted    : __sorted,
                validated : true,
                column    : "__column_name"
            }
        }
    }'
}