{
    'refresh_seconds' : '1',
    'schema' : '{
        fields : {
            place : {
                type      : "boolean",
                indexed   : __indexed,
                sorted    : __sorted,
                validated : __validated,
                column    : "__column_name"
            }
        }
    }'
}