{
    'refresh_seconds' : '1',
    'schema' : '{
        fields : {
            date_range : {
                type      : "date_range",
                validated : true,
                from      : "__range_from",
                to        : "__range_to",
                pattern   : "yyyy/MM/dd HH:mm:ss.SSS"
            }
        }
    }'
}